#version 330
out vec4 FragColor;

in vec2 BorderCoord;
in vec3 DebugColor;

void main()
{
	
	if( BorderCoord.y > -1 &&  BorderCoord.y < 1 && BorderCoord.x > -1 && BorderCoord.x < 1){
		FragColor = vec4(0.0, 0.8, 1.0, 1.0);	
	
	}else{
		FragColor = vec4(1.0, 1.0, 1.0, 1.0);
	}

	//FragColor = vec4(BorderCoord.x, BorderCoord.y, 0, 1.0);
	//FragColor = vec4(DebugColor.x, DebugColor.y, DebugColor.z, 1.0);	
}